Here are stored database files.

For production environment it's `database_prod.sqlite3`, and for development it's `database_dev.sqlite3`
Also there is a cache stored for serializer's compiled yaml files.